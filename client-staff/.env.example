# Copy to .env
# Must be the LAN address of the API server, not localhost — phones resolve
# "localhost" to themselves, so localhost only ever works on the server itself.
VITE_API_URL=http://192.168.1.50:4000
